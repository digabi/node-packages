{
  "name": "@digabi/saml-mock",
  "version": "0.0.1",
  "author": "Matriculation Examination Board, Finland",
  "license": "EUPL-1.2",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/digabi/node-packages.git"
  },
  "main": "dist/index.js",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc --project ./tsconfig.build.json && cp -r ./src/certs ./dist/certs && cp -r ./src/templates ./dist/templates",
    "test": "node --test --import tsx ./__tests__/*.test.ts",
    "lint": "tsc --noEmit && eslint --ignore-path ../../.eslintignore ./",
    "prepare": "npm run build"
  },
  "publishConfig": {
    "access": "public"
  },
  "dependencies": {
    "@xmldom/xmldom": "^0.9.8",
    "express": "^5.1.0",
    "handlebars": "^4.7.8",
    "lodash": "^4.17.21",
    "moment": "^2.30.1",
    "xml-crypto": "^6.1.2",
    "xml-encryption": "^3.1.0"
  },
  "devDependencies": {
    "@node-saml/passport-saml": "5.0.1"
  }
}
